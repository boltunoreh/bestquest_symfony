<ul class="footer__list">
    {% for project in projects %}
        <li class="footer__item">
            <a class="footer__link" href="{{ path('app__project__show', {'slug': project.slug}) }}">{{ project.title }}</a>
        </li>
    {% endfor %}
</ul>